# SFA System AI Development Guidelines

## 核心定位

这是服务快消品区域经营管理的 SFA 系统。

所有功能都应帮助管理者完成：

目标管理 → 执行监督 → 异常发现 → 原因分析 → 动作调整 → 结果提升

系统价值不是记录行为或展示报表，而是帮助管理者发现问题并推动行动。

## 第一性原理与根因优先

先基于第一性原理理解问题本质，再讨论实现方式。先拆清楚用户目标、当前现象、期望结果和现实约束；如果这四者仍不清楚，不得直接进入方案设计或编码。

所有判断应优先基于证据定位根因，而不是直接对表象做补丁。证据不足时，应继续调查、补充验证，或明确说明当前结论中的不确定性，不得把猜测当结论推进实现。

默认采用能够消除根因的最小充分方案：既解决真实问题，又不额外引入不必要的复杂度。对于简单问题，禁止为了“更通用”“以后扩展”而提前增加层级、服务、框架、兼容分支、兜底分支或预留抽象。

临时补丁只能用于以下情况：紧急止血、外部兼容要求、或当前阶段确实无法直接修改根因。采用临时补丁时，必须同时记录根因是什么、为什么暂时不能直接修复、影响边界是什么、如何验证补丁有效，以及未来删除补丁的条件。不得把临时补丁表述为最终方案。

第一性原理不等于推翻现有架构重做。若问题可以在既有模块职责和既定边界内被简单、直接地解决，应优先复用现有实现，而不是为了“更干净”重建一套新结构。

## 当前版本边界

Makuku 1.0 仅建设价格智能能力。

以下能力不属于 1.0 范围，除非用户明确重新开放范围：

- 铺货、陈列、货架、OSA、缺货和可见度分析
- 多 Agent 编排和自动执行
- 复杂规则引擎、动作目录和预算池

不得为了未来扩展而提前增加 1.0 不需要的表、服务或抽象。

## 开发前必须确认

只有在业务目标、需求范围和验收标准足够明确时才开始开发。只要存在可能影响架构、数据模型、用户体验、实现范围或验收结果的歧义，必须先向用户提问并确认，不得自行假设后开工。

开发前应确认：

- 业务目标是什么？
- 使用角色是谁？
- 实际业务动作是什么？
- 要识别什么异常？
- 验收标准是什么？

不要询问不影响方案的细节；这类细节可遵循现有代码和产品模式作出保守判断。

## 产品与数据原则

- 优先服务区域负责人的经营决策
- 不只展示数据，要支持问题发现、原因分析和行动
- 数据设计必须支持历史追踪和后续分析
- 避免孤立数据表和只服务单个页面的数据结构
- 明确事实数据、维度数据和分析指标的职责
- 尊重已经冻结的表职责和架构边界

## 较大需求的输出顺序

1. 需求理解
2. 风险和问题
3. 推荐方案
4. 数据设计与数据流
5. 功能、流程和权限设计
6. 技术实现

## 技术要求

- 简单可靠、可维护、可扩展
- 禁止临时性 Demo 架构
- 优先复用现有模式，避免无实际价值的抽象
- 修改后必须补充或运行与风险相匹配的测试
- 不得擅自修改或删除用户已有改动

## 架构与复用要求

- 开发前先检查相关系统架构、数据分层和现有组件模式
- 优先在现有模块职责内实现，不得随意跨层或新增平行架构
- 数据设计必须遵循既定粒度、迁移方式、权限边界和数据流方向
- 新建组件前必须搜索现有实现，优先直接复用或合理扩展
- 不得复制形成职责相近的组件，也不得为少量重复提前设计复杂抽象
- 当现有共享文件或组件职责过多时，新功能应按领域渐进拆分，不得继续无边界堆积
- 架构文档必须区分当前实现、目标方向和尚未落地的设计

## 详细业务背景

仅在任务涉及对应领域时读取：

- [SFA 业务总览](docs/business/sfa-overview.md)
- [系统架构](docs/architecture/system-architecture.md)
- [数据设计原则](docs/architecture/data-principles.md)
- [Makuku 1.0 价格架构](docs/architecture/price-intelligence-v1.md)
- [组件复用规则](docs/frontend/component-guidelines.md)
